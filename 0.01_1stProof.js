// el punto y coma es para poner variables en la misma linea
var firstColor = 200
var secondColor = 150

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0, firstColor, secondColor);
}
